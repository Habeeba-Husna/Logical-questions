/**
 * @param {string} s
 * @return {number}
 */
var countSegments = function(s) {
    let a;
     if (s.trim() === "") {
        return 0;
    }
   return a =s.trim().split(/\s+/).length
  console.log(a)
   
   
    
};